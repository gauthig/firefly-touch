# firefly-touch partition table — 16 MB flash, single factory app.
#
# Firmware updates are USB-flashed only (no OTA — see CLAUDE.md "OTA / Wi-Fi
# update path"); a dual-OTA layout would only add complexity with no benefit.
#
# App partition is 4 MB against a current app of ~0.65 MB — generous room for
# future LVGL assets. ~11 MB remains for the storage partition (custom icon
# fonts, captured bus logs, config).
#
# App partitions must start on a 64 KB (0x10000) boundary.
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x400000,
storage,    data, spiffs,  0x410000, 0xBF0000,
